# 服务端口
PORT=3001

# 运行时数据目录(图片/配置/会话);默认相对 server 目录的 ../data
DATA_DIR=

# 千问 API Key —— 作为兜底默认值。
# 优先级:前端设置页填入的密钥(config.json)优先;前端未提供时使用此处的值。
DASHSCOPE_API_KEY=

# 千问 Base URL(可被 config.json 覆盖)
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com
